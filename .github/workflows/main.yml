name: Gitee ↔ GitHub 双向同步（适配Gitee Token）

on:
  push:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4.1.1
        with:
          fetch-depth: 0

      - name: 配置Git用户信息
        run: |
          git config --global user.name "jingyisi"
          git config --global user.email "你的邮箱@example.com"

      - name: 添加Gitee远程仓库（正确认证方式）
        run: |
          git remote add gitee https://gitee.com/jingyisi/jingyisi-my-project.git
          git config credential.helper store
          echo "https://jingyisi:${{ secrets.GITEE_TOKEN }}@gitee.com" > ~/.git-credentials

      - name: 拉取Gitee最新代码（适配分支）
        run: |
          git pull gitee --rebase --allow-unrelated-histories || true

      - name: 推送到Gitee
        run: git push gitee --all --force-with-lease
      
      - name: 推送到GitHub
        run: git push origin --all --force-with-lease
